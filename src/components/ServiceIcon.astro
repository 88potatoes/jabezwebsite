---
const {text, link} = Astro.props;
---
<div class="d-flex flex-column justify-content-center align-items-center">
    <svg width="100" height="100">
        <image xlink:href={link} width="100" height="100"/>
    </svg>
    <p class="text">{text}</p>
</div>